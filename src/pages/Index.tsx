
import RoadTimeline from "../components/RoadTimeline";
import FloatingHearts from "../components/FloatingHearts";

const Index = () => {
  return (
    <div className="relative min-h-screen bg-gradient-to-b from-pink-50 via-white to-pink-50">
      <FloatingHearts />
      <RoadTimeline />
    </div>
  );
};

export default Index;
